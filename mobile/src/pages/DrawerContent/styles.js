import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    itemsScroll: {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
    },

    itemsList: {
        height: '50%',
        justifyContent: 'space-between',
    },

    itemText: {
        fontSize: 20,
    }
})